import styled from 'styled-components';
import { titleFont, yellow } from '../utilities';

export const Title = styled.h2`
  ${titleFont};
  color: ${yellow};
  font-size: 3rem;
`;

export const Subtitile = styled.h3`
  font-size: 1.5rem;
`;
